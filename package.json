{
    "name": "campus-events",
    "version": "1.0.0",
    "description": "Local Tourism Promotion Website",
    "source": "src/index.html",
    "scripts": {
        "start": "parcel src/*.html",
        "build": "parcel build src/*.html --dist-dir dist --no-cache"
    },
    "devDependencies": {
        "parcel": "^2.16.4"
    }
}
